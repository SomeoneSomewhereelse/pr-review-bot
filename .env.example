# Copy this file to .env and fill in real values. .env is gitignored.

# --- Database ---
# Postgres connection string (Supabase or self-hosted). Also where
# scripts/set_provider.py writes the DB-backed provider override (see
# SETUP.md §3.6) -- pointed at a local Postgres, that override is local only;
# it never reaches production unless this URL itself is the production one.
DATABASE_URL=

# --- Public Base URL ---
# Optional; auto-populated on Render as RENDER_EXTERNAL_URL.
PUBLIC_BASE_URL=

# --- GitHub App ---
# Numeric App ID: App settings -> General -> "App ID". See SETUP.md §1, which
# also explains why this is not the Client ID or the Installation ID.
GITHUB_APP_ID=
# Optional: the app auto-discovers this at boot when left empty.
GITHUB_APP_INSTALLATION_ID=
# Local development: path to the downloaded private-key PEM (kept out of git; see .gitignore).
GITHUB_APP_PRIVATE_KEY_PATH=./github-app-private-key.pem
# Render/hosted: base64-encoded private key (set in dashboard, never committed).
GITHUB_APP_PRIVATE_KEY_B64=
GITHUB_WEBHOOK_SECRET=
# Target repository for pull requests (owner/repo format)
GITHUB_TARGET_REPO=

# --- LLM provider ---
# One of: gemini | groq | vertex
LLM_PROVIDER=gemini

# Each credential below can have numbered siblings -- BASE_1, BASE_2, ... --
# swappable at runtime with no redeploy via `scripts/set_provider.py`'s
# key-index override (see README "Swapping API keys without a redeploy").
# Uncomment and fill in only the slots you actually plan to use; an unfilled
# slot is simply never selected, and adding one later needs one redeploy to
# provision (or a manual push -- see the Open Items note on that gap).

# gemini (AI-Studio, free tier, no card required)
GEMINI_API_KEY=
# GEMINI_API_KEY_1=
# GEMINI_API_KEY_2=

# groq (free tier fallback / cross-vendor demo)
GROQ_API_KEY=
# GROQ_API_KEY_1=
# GROQ_API_KEY_2=
# groq's own model var — see note below on why this isn't shared with LLM_MODEL.
GROQ_MODEL=llama-3.3-70b-versatile

# vertex (Vertex AI via google-genai's vertexai=True -- needs GCP billing)
# The credential is a GCP service-account JSON key, not an API-key string.
# Render/hosted: base64 of the whole key file (set in the dashboard, never committed).
GCP_SERVICE_ACCOUNT_KEY_B64=
# GCP_SERVICE_ACCOUNT_KEY_B64_1=
# GCP_SERVICE_ACCOUNT_KEY_B64_2=
# Local development: path to the downloaded key JSON (kept out of git; see .gitignore).
# Selected by the SAME key index as the B64 slots above; B64 wins when both
# resolve. With neither set, google-auth falls back to your own
# `gcloud auth application-default login` credentials.
GCP_SERVICE_ACCOUNT_KEY_PATH=./gcp-service-account-key.json
# GCP_SERVICE_ACCOUNT_KEY_PATH_1=
# GCP_SERVICE_ACCOUNT_KEY_PATH_2=
# Optional: derived from the service-account key's own project_id when unset.
GCP_PROJECT=
# Optional: which Vertex regional endpoint to call.
GCP_LOCATION=us-central1

# Model alias for the google-genai providers (gemini and vertex) ONLY. groq
# has its own model var above — one shared LLM_MODEL stopped making sense
# once a second unrelated provider family entered the picture — pinnable to
# a dated version for demo reproducibility.
# NOTE: this default is AI-Studio's alias and confirmed NOT to exist as a
# Vertex publisher model on at least one real project (404) -- if you're
# running LLM_PROVIDER=vertex, override this to a model your project's
# Vertex catalog actually has (confirmed working: gemini-2.5-flash). See
# SETUP.md section 2 for how to check your own project's catalog.
LLM_MODEL=gemini-flash-latest

# --- Optional operator tooling (NOT used by the deployed service) ---
# Read only by scripts/deploy.py on your own machine. Never add these to
# render.yaml and never set them on the Render service. Leaving any of them
# empty simply skips the check it enables.
# Render API key (Account Settings -> API Keys): enables the render-service
# check and `--sync-env`.
RENDER_API_KEY=
# Render service name; must match `name:` in render.yaml.
RENDER_SERVICE_NAME=pr-review-engine
# UptimeRobot read-only API key: enables the uptime-pinger check.
UPTIMEROBOT_API_KEY=

# --- Optional: proactive per-key daily usage cap ---
# Unset by default (cap off). When set, the dispatcher refuses to START a
# review once the ACTIVE (provider, key slot) has already used this much
# since the last reset, deferring the ticket to the next reset instead.
# KEY_USAGE_TOKEN_CAP wins outright when both are set -- the cost cap is
# then not consulted at all. See README "Capping how much one API key may
# spend per day".
# KEY_USAGE_TOKEN_CAP=20000
# KEY_USAGE_COST_CAP_USD=0.50
# When the usage day rolls over, UTC. Any HH:MM or HH:MM:SS -- not
# whole-hours-only, so a demo can set a reset a couple of minutes out.
KEY_USAGE_RESET_TIME_UTC=04:00
